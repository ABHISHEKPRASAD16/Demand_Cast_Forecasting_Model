# Runtime dependencies for the FastAPI service only - deliberately excludes
# the training/pipeline stack (torch, prophet, dbt, kaggle, statsmodels,
# great-expectations, evidently). Those are dev/pipeline-time tools; pulling
# them into the serving image would drag in a C++ toolchain for prophet's
# cmdstan and hundreds of MB for torch, for a service that never trains
# anything. See requirements.txt for the full dev/pipeline set.
pandas>=2.2
pydantic>=2.7
pyyaml>=6.0
duckdb>=1.0
lightgbm>=4.3
mlflow>=3.0
fastapi>=0.111
uvicorn[standard]>=0.30
