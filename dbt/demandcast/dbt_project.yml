name: "demandcast"
version: "1.0.0"
config-version: 2

profile: "demandcast"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
clean-targets: ["target", "dbt_packages"]

vars:
  raw_data_dir: "../../data/raw"

models:
  demandcast:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
